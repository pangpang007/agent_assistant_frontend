# Phase 2 前端实施计划

> 状态：**已完成**

## 实施步骤

- [x] Step 1: 类型定义（`agent` / `tool` / `model`）
- [x] Step 2: Service 层（`agentService` / `toolService` / `modelService`）
- [x] Step 3: 共享组件（`DeleteConfirmModal` / `FormSelect`）
- [x] Step 4: Agent 组件（`AgentCard` / `TemperatureSlider` / `ToolMultiSelect`）
- [x] Step 5: Agent 页面（列表 / 表单 / 详情）
- [x] Step 6: Tool 组件（`ToolCard` / `SwaggerImporter`）
- [x] Step 7: Tool 页面（列表 / 表单 / 测试）
- [x] Step 8: Model 组件（`ModelSupplierCard` / `ModelListItem`）
- [x] Step 9: `ModelSettingsPage`
- [x] Step 10: 路由与 `menuConfig` 更新（`/settings/models`）
- [x] Step 11: 构建与 lint 验证

## 验收清单

- [x] `/agents` 列表、创建、编辑、预置查看
- [x] `/tools` 列表、Swagger 导入创建、编辑、测试
- [x] `/settings/models` 供应商 CRUD、默认模型、用量统计
- [x] 删除确认 Modal（含引用计数警告）
- [x] 表单离开拦截（`useBlocker` + `beforeunload`）
- [x] 设计 token 样式（`phase2.css`）

## 后端依赖

API 前缀 `/api`，开发环境经 Vite proxy 转发至 `http://localhost:8000`。

主要端点：`/agents`、`/tools`、`/models/providers`、`/models/usage`、`/models/enabled`。
