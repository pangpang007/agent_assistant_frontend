---
AIGC:
    Label: "1"
    ContentProducer: 001191110102MACQD9K64018705
    ProduceID: 4263223131904378_0/project_7661866342080954651-files/产品设计/汤圆的代码助手_设计系统规范.md
    ReservedCode1: ""
    ContentPropagator: 001191110102MACQD9K64028705
    PropagateID: 4263223131904378#1783927126909
    ReservedCode2: ""
---
# 汤圆的代码助手 - 设计系统规范 v1.0

> 风格基调：GitHub Dark Theme | 英文字体：Consolas | 中文字体：系统默认

---

## 一、色彩体系

### 1.1 背景色
| Token | 色值 | 用途 |
|-------|------|------|
| `--bg-canvas` | `#0d1117` | 页面主背景 |
| `--bg-surface` | `#161b22` | 卡片/面板/侧边栏背景 |
| `--bg-elevated` | `#21262d` | 悬浮面板/弹窗/下拉菜单背景 |
| `--bg-input` | `#0d1117` | 输入框背景 |
| `--bg-hover` | `#1f2937` | 行/按钮 hover 背景 |
| `--bg-active` | `#2d333b` | 选中/激活状态背景 |

### 1.2 文字色
| Token | 色值 | 用途 |
|-------|------|------|
| `--text-primary` | `#e6edf3` | 标题/正文 |
| `--text-secondary` | `#8b949e` | 辅助文字/描述 |
| `--text-tertiary` | `#6e7681` | 占位符/禁用文字 |
| `--text-inverse` | `#ffffff` | 深色背景上的白色文字（按钮等） |

### 1.3 边框色
| Token | 色值 | 用途 |
|-------|------|------|
| `--border-default` | `#30363d` | 默认边框 |
| `--border-muted` | `#21262d` | 弱化边框（分割线） |
| `--border-emphasis` | `#8b949e` | 强调边框（focus 状态） |

### 1.4 功能色/语义色
| Token | 色值 | 用途 |
|-------|------|------|
| `--accent-primary` | `#58a6ff` | 主操作色（链接/按钮/选中态） |
| `--accent-success` | `#3fb950` | 成功状态 |
| `--accent-warning` | `#d29922` | 警告状态 |
| `--accent-danger` | `#f85149` | 危险/错误状态 |
| `--accent-info` | `#58a6ff` | 信息提示 |

### 1.5 节点状态色（工作流画布专用）
| 状态 | 色值 | 说明 |
|------|------|------|
| 等待中 | `#6e7681` | 灰色 |
| 执行中 | `#58a6ff` | 蓝色（带旋转动画） |
| 成功 | `#3fb950` | 绿色 |
| 失败 | `#f85149` | 红色 |
| 等待审核 | `#d29922` | 橙色 |
| 已跳过 | `#484f58` | 浅灰色 |

### 1.6 亮色模式（可选，后续实现）
亮色模式参考 GitHub Light：
| Token | 色值 |
|-------|------|
| `--bg-canvas` | `#ffffff` |
| `--bg-surface` | `#f6f8fa` |
| `--bg-elevated` | `#ffffff` |
| `--text-primary` | `#1f2328` |
| `--text-secondary` | `#656d76` |
| `--border-default` | `#d0d7de` |
| `--accent-primary` | `#0969da` |

---

## 二、字体规范

### 2.1 字体族
```css
--font-sans: -apple-system, BlinkMacSystemFont, "PingFang SC", "Microsoft YaHei", "Helvetica Neue", sans-serif;
--font-mono: "Consolas", "Monaco", "Courier New", monospace;
```

- **UI 文字**：`--font-sans`（中文走系统默认，英文走系统默认）
- **代码/等宽**：`--font-mono`（Consolas 优先）
- **产品名称/品牌**：`--font-mono`（Consolas，强化技术感）

### 2.2 字号体系
| Token | 大小 | 行高 | 用途 |
|-------|------|------|------|
| `--text-xs` | 12px | 16px | 辅助标签/badge |
| `--text-sm` | 14px | 20px | 正文/列表/表格 |
| `--text-base` | 16px | 24px | 卡片标题/重要正文 |
| `--text-lg` | 20px | 28px | 页面子标题 |
| `--text-xl` | 24px | 32px | 页面主标题 |
| `--text-2xl` | 32px | 40px | 大标题/Dashboard 数字 |

### 2.3 字重
| Token | 值 | 用途 |
|-------|-----|------|
| `--font-normal` | 400 | 正文 |
| `--font-medium` | 500 | 强调文字/按钮 |
| `--font-semibold` | 600 | 标题 |
| `--font-bold` | 700 | 大标题/数字 |

---

## 三、间距系统

基于 4px 栅格：
| Token | 值 | 用途 |
|-------|-----|------|
| `--space-1` | 4px | 图标与文字间距 |
| `--space-2` | 8px | 紧凑元素间距 |
| `--space-3` | 12px | 表单元素内间距 |
| `--space-4` | 16px | 卡片内间距/组件间距 |
| `--space-5` | 20px | 区块内间距 |
| `--space-6` | 24px | 卡片间距/模块间距 |
| `--space-8` | 32px | 大区块间距 |
| `--space-10` | 40px | 页面级间距 |
| `--space-12` | 48px | 页面顶/底部留白 |

---

## 四、圆角规范

| Token | 值 | 用途 |
|-------|-----|------|
| `--radius-sm` | 4px | 小按钮/tag/badge |
| `--radius-md` | 6px | 输入框/小按钮 |
| `--radius-lg` | 8px | 卡片/面板 |
| `--radius-xl` | 12px | 弹窗/大面板 |
| `--radius-full` | 9999px | 圆形头像/圆形按钮 |

---

## 五、阴影规范

| Token | 值 | 用途 |
|-------|-----|------|
| `--shadow-sm` | `0 1px 2px rgba(0,0,0,0.3)` | 卡片默认 |
| `--shadow-md` | `0 4px 12px rgba(0,0,0,0.4)` | 悬浮/下拉菜单 |
| `--shadow-lg` | `0 8px 24px rgba(0,0,0,0.5)` | 弹窗 |
| `--shadow-focus` | `0 0 0 3px rgba(88,166,255,0.3)` | 输入框 focus 光晕 |

---

## 六、动效规范

| Token | 值 | 用途 |
|-------|-----|------|
| `--duration-fast` | 100ms | hover 颜色变化 |
| `--duration-normal` | 200ms | 按钮状态切换 |
| `--duration-slow` | 300ms | 面板展开/折叠 |
| `--duration-modal` | 150ms | 弹窗进出 |
| `--easing-default` | `cubic-bezier(0.4, 0, 0.2, 1)` | 通用 |
| `--easing-enter` | `cubic-bezier(0, 0, 0.2, 1)` | 进入 |
| `--easing-exit` | `cubic-bezier(0.4, 0, 1, 1)` | 退出 |

---

## 七、布局规范

### 7.1 全局布局
```
┌──────────────────────────────────────────────┐
│ 顶部导航栏 (h=56px, bg=--bg-surface)         │
├────────┬─────────────────────────────────────┤
│ 左侧   │                                     │
│ 导航   │    内容区域                           │
│ 菜单   │    (padding: 24px)                   │
│ (w=240)│                                     │
│        │                                     │
├────────┴─────────────────────────────────────┤
│ （无底部栏，中台风格）                         │
└──────────────────────────────────────────────┘
```

### 7.2 断点
| 名称 | 值 | 说明 |
|------|-----|------|
| `sm` | 640px | 最小支持（侧边栏折叠为图标） |
| `md` | 768px | 平板 |
| `lg` | 1024px | 小屏笔记本 |
| `xl` | 1280px | 标准桌面（主目标） |
| `2xl` | 1536px | 大屏 |

### 7.3 内容区最大宽度
- 表单页：max-width 640px，居中
- 列表页：max-width 1200px
- 工作流编辑器：全宽（无 max-width 限制）
- Dashboard：max-width 1200px

---

## 八、基础组件规范

### 8.1 Button
| 类型 | 样式 | 用途 |
|------|------|------|
| Primary | bg=#58a6ff, text=#fff, hover bg=#79c0ff | 主操作（保存/运行/创建） |
| Default | border=#30363d, text=#e6edf3, hover bg=#21262d | 次要操作 |
| Danger | bg=#f85149, text=#fff, hover bg=#ff7b72 | 危险操作（删除） |
| Ghost | 无边框无背景, hover bg=#1f2937 | 弱操作（取消/更多） |

- 高度：sm=28px, md=34px, lg=40px
- 圆角：`--radius-md`
- 内间距：`--space-3` 水平，`--space-1` 垂直
- 禁用态：opacity 0.5, cursor not-allowed

### 8.2 Input
- 背景：`--bg-input`
- 边框：`--border-default`
- Focus：边框变 `--accent-primary` + shadow-focus
- 错误：边框变 `--accent-danger`
- 高度：sm=28px, md=34px, lg=40px
- 圆角：`--radius-md`
- 占位符色：`--text-tertiary`

### 8.3 Card
- 背景：`--bg-surface`
- 边框：`--border-default`
- 圆角：`--radius-lg`
- 内间距：`--space-5`
- 默认无阴影，hover 时加 `--shadow-sm`

### 8.4 Modal/Dialog
- 背景：`--bg-elevated`
- 边框：`--border-default`
- 圆角：`--radius-xl`
- 阴影：`--shadow-lg`
- 遮罩：rgba(0,0,0,0.6) + backdrop-filter: blur(4px)
- 宽度：sm=400px, md=560px, lg=720px

### 8.5 Table
- 表头背景：`--bg-surface`
- 行背景：交替 `--bg-canvas` / `--bg-surface`（可选）
- 行 hover：`--bg-hover`
- 边框：`--border-muted`（行分割线）
- 单元格内间距：`--space-3` 垂直，`--space-4` 水平

### 8.6 Tag/Badge
- 背景：`--bg-elevated`
- 边框：`--border-default`
- 圆角：`--radius-sm`
- 字号：`--text-xs`
- 内间距：2px 8px

### 8.7 Toast/Notification
- 从右上角弹出
- 背景：`--bg-elevated`
- 左侧色条：success=绿, error=红, warning=黄, info=蓝
- 自动消失：3秒（可配置）
- 进场动画：从右滑入

### 8.8 导航菜单项
- 默认：text=`--text-secondary`
- Hover：bg=`--bg-hover`, text=`--text-primary`
- Active：bg=`--bg-active`, text=`--text-primary`, 左侧 2px 蓝条
- 高度：36px
- 图标 + 文字，图标 16px

### 8.9 空状态
- 居中展示
- 图标（64px，灰色）
- 标题（`--text-primary`，`--text-base`）
- 描述（`--text-secondary`，`--text-sm`）
- 操作按钮（可选）

### 8.10 加载状态
- 页面级：骨架屏（`--bg-elevated` 色块 + shimmer 动画）
- 组件级：Spinner（`--accent-primary` 色旋转圆环）
- 按钮内：按钮文字替换为 Spinner

---

## 九、图标规范

- 图标库：Lucide React（轻量、风格统一、GitHub 风）
- 尺寸：16px（导航/菜单）、20px（按钮内）、24px（标题旁）
- 颜色：跟随文字色 token
- 描边宽度：1.5px

---

## 十、工作流画布专属规范

### 10.1 画布背景
- 背景色：`--bg-canvas`
- 网格点：`#21262d`，间距 20px
- 选中区域框：`--accent-primary` 半透明填充

### 10.2 节点样式
- 宽度：240px（固定）
- 背景：`--bg-surface`
- 边框：`--border-default`
- 圆角：`--radius-lg`
- 选中态：边框 `--accent-primary` + shadow-focus
- 执行中：边框动画（蓝色流光效果）
- 标题栏：节点类型图标 + 名称 + 状态指示点
- 内容区：简要配置信息（可展开）
- 连接点：8px 圆形，hover 放大到 12px

### 10.3 连线样式
- 默认：`--border-default`，2px，贝塞尔曲线
- 选中/活跃：`--accent-primary`，2.5px
- 数据流动画：执行中连线上有蓝色粒子流动效果
- 错误连线：`--accent-danger`，虚线

### 10.4 节点库面板
- 可折叠（左侧）
- 分类分组：基础/AI能力/逻辑控制/交互控制/数据处理/外部集成
- 每个节点项：图标 + 名称 + 一句话描述
- 拖拽到画布时有半透明预览

### 10.5 属性面板
- 右侧固定宽度 320px
- 可折叠
- 表单布局：label 在上，input 在下
- 变量选择器：下拉选择，显示 `${节点名.变量名}`

---

## 十一、暗色/亮色切换

- 默认：暗色模式
- 切换按钮位置：顶部导航栏右侧（太阳/月亮图标）
- 切换方式：CSS Variables 切换 data-theme 属性
- 持久化：localStorage 存储用户偏好
- 切换动画：全局 300ms fade 过渡

---

> 本内容由 Coze AI 生成，请遵循相关法律法规及《人工智能生成合成内容标识办法》使用与传播。
