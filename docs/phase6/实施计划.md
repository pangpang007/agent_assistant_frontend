# Phase 6 前端实施计划

> 状态：**已完成**

## 实施步骤

- [x] Step 1: `types/phase6.ts` + 工具 `versionDiff`
- [x] Step 2: API — `templateService` / `versionService` / `logService` / `envService`，扩展 `executionService` 历史接口
- [x] Step 3: Store — `templateStore` / `executionHistoryStore` / `logStore` / `envVarStore`（避开 Phase 5 `executionStore` 命名冲突）
- [x] Step 4: 路由与侧边栏菜单（`/templates`、`/executions`、重定向）
- [x] Step 5: 环境变量页 CRUD + SecretMask
- [x] Step 6: 模板库 + 另存为模板弹窗
- [x] Step 7: 版本侧边栏 + 回滚确认
- [x] Step 8: 版本对比视图
- [x] Step 9: 执行历史列表
- [x] Step 10: 执行详情（画布快照 + 时间线 + 节点详情）
- [x] Step 11: 日志中心
- [x] Step 12: 单元测试、`tsc`、文档补全

## 验收清单

- [x] `/templates` 可筛选并使用模板创建工作流
- [x] 编辑器可打开版本侧边栏、回滚与对比
- [x] 编辑器可将当前工作流另存为模板
- [x] `/executions` 列表筛选分页，详情可查看节点与日志时间线
- [x] `/logs` 全局日志筛选与详情
- [x] `/settings/env` 字符串/密钥变量增删改查，Secret 脱敏
- [x] 旧菜单路径重定向到新页面

## 后端依赖

| 模块 | 主要路径 |
|------|---------|
| 模板 | `/api/templates`、`/api/templates/:id/use`、`/api/workflows/:id/save-as-template` |
| 版本 | `/api/workflows/:id/versions`、`.../rollback`、`.../diff` |
| 执行历史 | `/api/executions`、`/api/executions/:id` |
| 日志 | `/api/logs` |
| 环境变量 | `/api/env-vars` |
