# Phase 4 前端实施计划

> 状态：**已完成**

## 实施步骤

- [x] Step 1: 安装依赖（`@xyflow/react`, `dagre`, `@monaco-editor/react`）
- [x] Step 2: `EditorLayout` + 路由拆分（列表 AppLayout / 编辑器 EditorLayout）
- [x] Step 3: `workflowEditorStore` + `workflowService` + 类型定义
- [x] Step 4: 节点默认配置、校验、变量解析、自动布局工具
- [x] Step 5: `BaseNode` + 16 种节点 + `nodeTypes` 注册
- [x] Step 6: `NodeLibrary` + `WorkflowCanvas`（拖拽、连线、MiniMap）
- [x] Step 7: 16 种属性面板 + `VariableSelector`
- [x] Step 8: `WorkflowToolbar` / `WorkflowStatusBar` / `ContextMenu`
- [x] Step 9: `WorkflowEditorPage`（保存、运行、版本、快捷键、离开确认）
- [x] Step 10: `WorkflowListPage` + `WorkflowCard` + 导入
- [x] Step 11: `DebugPanel` + `useWorkflowWebSocket`
- [x] Step 12: 构建与 lint 验证

## 验收清单

- [x] `/workflows` 列表：搜索、新建、导入、删除、卡片操作
- [x] `/workflows/:id` 全屏编辑器三栏布局
- [x] 16 种节点拖拽创建、选中、属性配置
- [x] 连线、撤销/重做、自动布局、右键菜单
- [x] 前端校验（连通性、环、变量引用、必填项）
- [x] 保存 / 版本切换 / 导入导出 JSON
- [x] 单节点调试面板
- [x] WebSocket 执行状态监控（含重连）

## 后端依赖

API 前缀 `/api`，主要端点：`/workflows`、`/workflows/:id/save`、`/workflows/:id/run`、`/workflows/:id/test-node`。

WebSocket：`ws://localhost:8000/api/executions/:id/stream`
